{% extends 'base.html.twig' %}

{% block title %}Jednostki{% endblock %}

{% block body %}

<div class="container">
    <h1 class="m-5 text-center">Jednostki</h1>

    <div class="row">
        <div class="col-md-8">
            {% if form is defined %}
            {{ form_start(form) }}
            {{ form_widget(form) }}
            {{ form_end(form) }}
            {% endif %}
        </div>
        <div class="col-md-4">
            <table class="table">
            <thead>
                <tr>
                <th scope="col">#</th>
                <th scope="col">Nazwa</th>
                <th scope="col">Skrót</th>
                </tr>
            </thead>
            <tbody>
            {% for unit in units %}
                <tr>
                <th scope="row">{{ unit.id }}</th>
                <td>{{ unit.name }}</td>
                <td>{{ unit.short }}</td>
                </tr>
                {% endfor %}
            </tbody>
            </table>
        </div>
    </div>


</div>

{% endblock %}
