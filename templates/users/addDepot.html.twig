{% extends 'base.html.twig' %}

{% block title %}Dodaj magazyn{% endblock %}

{% block body %}
    <div class="container-fluid pt-5">
        <div class="row">
            <div class="col-12">
                {% if form is defined %}
                {{ form(form) }}
                {% endif %}
            </div>
        </div>
    </div>
{% endblock %}

